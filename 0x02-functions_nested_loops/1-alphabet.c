#include "main.h"
/**
 * main - prints the alphabet in lowercase
 * Return: return print_alphabet
 */
void print_alphabet(void)
{
	char n;

	for (n = 'a' ; n <= 'z' ; n++)
	_putchar(n);
	_putchar('\n');
}
