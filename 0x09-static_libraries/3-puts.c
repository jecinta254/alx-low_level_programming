#include "main.h"
#include<stdio.h>

/**
 * main - Entry point parameter
 * Return: Return 0 (Success)
 */

int main(void)

{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
